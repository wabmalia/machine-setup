# Managed by chezmoi. Uses pinentry-mac so passphrase prompts show up as a
# native macOS dialog (works from GUI git clients/editors, not just a TTY).
# Assumes Apple Silicon Homebrew prefix; Intel Macs would use /usr/local/bin.
pinentry-program /opt/homebrew/bin/pinentry-mac
